# Security policy

このデモへ実キー、平文パスワード、機密情報、個人情報、実際の現場記録を登録しない。

漏えいが疑われる場合は、先にアプリを停止し、OpenAI APIキーを失効・ローテーションする。パスワード漏えい時は参加者／管理者パスワードと`AUTH_VERSION`を変更する。Gitからファイルを削除するだけで対処完了としない。

問題の報告にはsafe error code、操作IDの先頭8文字、発生時刻、再現手順を含める。APIキー、平文パスワード、Cloud Secrets、DB、全プロンプトをIssueやチャットへ貼らない。

この初期版は共有パスワード、単一プロセス、Cloudの一時SQLiteを前提とする。組織向け認証、長期監査、機密データ、複数replicaには未対応。
